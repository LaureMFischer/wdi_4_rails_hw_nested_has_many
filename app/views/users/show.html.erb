<dl>
  <dt>Name</dt><dd><%= @user.name %></dd>
  <dt>Email</dt><dd><%= @user.email %></dd>

  <dt>Articles</dt><dd><% @articles.each do |article| %>
    <%= article.title %><br>
    Article preview: <%= article.body %>
    <% end %>
  </dd>
</dl>

<%= link_to 'New Article', new_user_article_path(@user_id) %>
